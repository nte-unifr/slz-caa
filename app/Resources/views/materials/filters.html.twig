<fieldset>
    <legend>{{ 'general'|trans }}</legend>
    <div class="form-group">
        <label for="fiLanguage">{{ 'language'|trans }}</label><br>
        <select id="fiLanguage" class="js-chosen" data-col="6">
            {% for language in languages %}
                <option value="{{ language }}">{{ language|trans({}, "languages") }}</option>
            {% endfor %}
        </select>
        <p class="help-block">{{ 'language_info'|trans }}</p>
    </div>
    <div class="form-group">
        <label for="fiTheme">{{ 'thematic'|trans }}</label><br>
        <select id="fiTheme" class="js-chosen" multiple>
            <optgroup label="-">
                {% for theme in themes.no %}
                    <option value="" selected>{{ theme|trans({}, "themes") }}</option>
                {% endfor %}
            </optgroup>
            <optgroup label="Quotidien">
                {% for theme in themes.everyday %}
                    <option value="{{ theme }}" selected>{{ theme|trans({}, "themes") }}</option>
                {% endfor %}
            </optgroup>
            <optgroup label="Sciences des lettres">
                {% for theme in themes.humanities %}
                    <option value="{{ theme }}" selected>{{ theme|trans({}, "themes") }}</option>
                {% endfor %}
            </optgroup>
            <optgroup label="Sciences naturelles">
                {% for theme in themes.naturalscience %}
                    <option value="{{ theme }}" selected>{{ theme|trans({}, "themes") }}</option>
                {% endfor %}
            </optgroup>
        </select>
    </div>
    <div class="form-group">
        <label for="fiModality">{{ 'learning_modality'|trans }}</label><br>
        <select id="fiModality" class="js-chosen" multiple>
            {% for modality in modalities %}
                <option value="{{ modality }}" selected>{{ modality|trans({}, "modalities") }}</option>
            {% endfor %}
        </select>
        <p class="help-block">{{ 'reference_books_examples'|trans }}</p>
    </div>
</fieldset>
<fieldset>
    <legend>{{ 'level_of_language'|trans }}</legend>
    <div class="row">
        <div class="col-xs-12 col-sm-6">
            <div class="form-group">
                <label for="fiVocabulary">{{ "wortschatz"|trans }}</label><br>
                <select id="fiVocabulary" class="js-chosen">
                    {% for level in levels %}
                        <option value="{{ level }}">{{ level|trans({}, "levels") }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
        <div class="col-xs-12 col-sm-6">
            <div class="form-group">
                <label for="fiReading">{{ "lesen"|trans }}</label><br>
                <select id="fiReading" class="js-chosen">
                    {% for level in levels %}
                        <option value="{{ level }}">{{ level|trans({}, "levels") }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-xs-12 col-sm-6">
            <div class="form-group">
                <label for="fiListening">{{ "hören"|trans }}</label><br>
                <select id="fiListening" class="js-chosen">
                    {% for level in levels %}
                        <option value="{{ level }}">{{ level|trans({}, "levels") }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
        <div class="col-xs-12 col-sm-6">
            <div class="form-group">
                <label for="fiSpeaking">{{ "sprechen"|trans }}</label><br>
                <select id="fiSpeaking" class="js-chosen">
                    {% for level in levels %}
                        <option value="{{ level }}">{{ level|trans({}, "levels") }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-xs-12 col-sm-6">
            <div class="form-group">
                <label for="fiPronunciation">{{ "aussprache"|trans }}</label><br>
                <select id="fiPronunciation" class="js-chosen">
                    {% for level in levels %}
                        <option value="{{ level }}">{{ level|trans({}, "levels") }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
        <div class="col-xs-12 col-sm-6">
            <div class="form-group">
                <label for="fiWriting">{{ "schreiben"|trans }}</label><br>
                <select id="fiWriting" class="js-chosen">
                    {% for level in levels %}
                        <option value="{{ level }}">{{ level|trans({}, "levels") }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-xs-12 col-sm-6">
            <div class="form-group">
                <label for="fiSpelling">{{ "orthografie"|trans }}</label><br>
                <select id="fiSpelling" class="js-chosen">
                    {% for level in levels %}
                        <option value="{{ level }}">{{ level|trans({}, "levels") }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
        <div class="col-xs-12 col-sm-6">
            <div class="form-group">
                <label for="fiGrammar">{{ "grammatik"|trans }}</label><br>
                <select id="fiGrammar" class="js-chosen">
                    {% for level in levels %}
                        <option value="{{ level }}">{{ level|trans({}, "levels") }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-xs-12 col-sm-6">
            <div class="form-group">
                <label for="fiTests">{{ "test"|trans }}</label><br>
                <select id="fiTests" class="js-chosen">
                    {% for level in levels %}
                        <option value="{{ level }}">{{ level|trans({}, "levels") }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
        <div class="col-xs-12 col-sm-6">
            <p class="help-block">{{ 'level_description'|trans }}:
                <a href="http://www.unifr.ch/centredelangues/autoapprentissage/Niveauraster/RasterSelbstbeurteilungDE.pdf"><label>DE</label></a>
                <a href="http://www.unifr.ch/centredelangues/autoapprentissage/Niveauraster/French-Francais.pdf"><label>FR</label></a>
                <a href="http://www.unifr.ch/centredelangues/autoapprentissage/Niveauraster/English.pdf"><label>EN</label></a>
                <a href="http://www.unifr.ch/centredelangues/autoapprentissage/Niveauraster/Italian-Italiano.pdf"><label>IT</label></a>
                <a href="http://www.unifr.ch/centredelangues/autoapprentissage/Niveauraster/Spanish.pdf"><label>ES</label></a>
            </p>
        </div>
    </div>
</fieldset>
<fieldset>
    <legend>{{ 'material'|trans }}</legend>
    <div class="form-group">
        <label for="fiSource">{{ 'source_language'|trans }}</label><br>
        <select id="fiSource" class="js-chosen" multiple>
            {% for language in languages_source %}
                <option value="{{ language }}" selected>{{ language|trans({}, "languages_source") }}</option>
            {% endfor %}
        </select>
    </div>
    <div class="form-group">
        <label for="fiType">{{ 'media'|trans }}</label><br>
        <select id="fiType" class="js-chosen" data-col="5" multiple>
            {% for media in medias %}
                <option value="{{ media }}" selected>{{ media|trans({}, "medias") }}</option>
            {% endfor %}
        </select>
    </div>
    <div class="form-group">
        <label for="fiYear">{{ 'year_of_publication'|trans }}</label><br>
        <select id="fiYear" class="js-chosen">
            {% for year in years %}
                <option value="{{ year }}" {% if year == "all" %}selected{% endif %}>{{ year|trans({}, "years") }}</option>
            {% endfor %}
        </select>
</fieldset>
