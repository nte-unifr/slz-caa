<table class="table table-striped table-hover" data-langde="{{ asset('json/German.json') }}" data-langfr="{{ asset('json/French.json') }}" data-langen="{{ asset('json/English.json') }}">
    <thead>
        <tr>
            <th></th><!-- id -->
            {% for field in fields %}
                <th>{{ field.name|trans({}, 'fields') }}</th>
            {% endfor %}
        </tr>
    </thead>
</table>
